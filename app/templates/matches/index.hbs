<div class="row">
  <h1>Matches</h1>

  {{#if sessionService.isAuthenticated}}
    <p>
      {{link-to "New Match" "matches.new" classNames="button tiny"}}
    </p>
  {{/if}}

  {{match-table
    items=this
    removeAction="remove"
  }}
</div>
