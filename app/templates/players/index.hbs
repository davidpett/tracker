<div class="row">
  <h1>Players</h1>

  {{#if session.isAuthenticated}}
    <p>
      {{link-to "New Player" "players.new" }}
    </p>
  {{/if}}

  {{player-table
    items=this
    hasTeam=true
    removeAction="remove"
  }}
</div>
