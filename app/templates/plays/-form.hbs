<form {{action "save" on="submit"}}>
  <div>
    <label>
      Body
      {{input type="text" value=body}}
    </label>
  </div>
  <div>
    <input type="submit" value="Save" class="button tiny">
  </div>
</form>
