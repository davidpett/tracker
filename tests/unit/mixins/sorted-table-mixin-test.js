import Ember from 'ember';
import SortedTableMixinMixin from 'tracker/mixins/sorted-table-mixin';

module('SortedTableMixinMixin');

// Replace this with your real tests.
test('it works', function() {
  var SortedTableMixinObject = Ember.Object.extend(SortedTableMixinMixin);
  var subject = SortedTableMixinObject.create();
  ok(subject);
});
